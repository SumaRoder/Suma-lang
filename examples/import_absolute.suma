import "core"
import "./lib/local_stats.suma"

pub main(): Int {
    score: Int = weighted_score(10, 20, 30)
    print("absolute score = " + to_str(score))
    print("absolute class = " + describe_score(score))
    print("even score = " + to_str(is_even(score)))
    return score
}
