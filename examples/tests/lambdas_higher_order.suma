pub apply_twice(f: Function, value: Int): Int {
    return f(f(value))
}

pub main(): Int {
    add_ten: Function = fn(x: Int): Int { return x + 10 }
    add_one: Function = fn(x: Int): Int { return x + 1 }
    return apply_twice(add_ten, 20) + apply_twice(add_one, 0)
}
